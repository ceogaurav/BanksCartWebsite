    // src/data/mockData.ts
    // This file is a placeholder to resolve import errors.
    // You should populate it with actual data as needed by your components.

    export const loanRates = [
      { type: 'Personal Loan', rate: '10.5%' },
      { type: 'Home Loan', rate: '8.0%' },
      { type: 'Car Loan', rate: '9.2%' },
      // Add more mock data if other components expect it from here
    ];

    // If other data is expected from mockData, add it here, e.g.:
    // export const someOtherData = [...];
    